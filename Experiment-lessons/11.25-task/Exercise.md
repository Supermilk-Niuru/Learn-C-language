## 题目（考查数组）
给定一字符串S（主串），编写函数，输出另一个字符串T（模式串）在S中所有出现的位置（下标），void strloc（char S[],char T[]）。
**例如**  
主串S为："abdsbcabcabbabccbab"  
模式串T为："ab"  
输出：0，3，7，10，13，18
#### 要求
分别采用“**不使用字符串库函数**”和“**使用字符串库函数**”的方式实现。  
可以自行探索一下**KMP算法**来求解。
